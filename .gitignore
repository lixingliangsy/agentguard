.vercel

# AI usage quota counters (local)
.data/

.env
.env.local
.env*.local

# Superpowers audit 2026-07-18: ensure secrets/local artifacts ignored
node_modules
.next
data/*.json
!.gitkeep
dev-local*.log

# stale build caches (added 2026-09-12 to prevent accidental commit)
.next_old/
.next_old2/
